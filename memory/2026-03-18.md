- 2026-03-18: quant_binance 완전 재학습 엔진 작업 진행. `policy_validation`이 외부 runner artifact(`validation_report.json` 등)를 읽어 evidence에 병합되도록 연결했고, `flush(...)` 시 persisted `policy_state.json`에도 반영되게 수정. 관련 커밋: `87269df` (`Wire validation runner evidence into policy state`). 전체 회귀: `Ran 206 tests in 43.907s`, `OK`.
- 2026-03-18: validation artifact 수동 배치 단계를 줄이기 위해 `quant_binance/validation_report.py`에 `build_policy_validation_runner_artifact(...)`, `write_policy_validation_runner_artifact(...)` 추가. `session.flush(...)`가 run 디렉터리에 `validation_report.json`이 없으면 자동 생성 후 evidence로 흡수하도록 변경. 관련 커밋: `542e3bb` (`Auto-generate policy validation artifacts`). 전체 회귀: `Ran 208 tests in 44.745s`, `OK`.
- 2026-03-18: 현재 다음 우선순위는 (1) candidate vs current 실제 비교 validation runner, (2) 그 비교 결과를 `promote/keep/demote/disable` 판정에 직접 연결, (3) 필요 시 execution penalty / macro bias까지 active policy 튜닝 범위 확대.

- User wants Telegram minimized to report-only delivery for this trading system: set `.env.local` with `TELEGRAM_NOTIFY_AUTO_SUMMARY=0` and `TELEGRAM_REPORT_ONLY=1` so AUTO_SUMMARY and operational fill/cooldown chatter do not get sent to the user, while report messages remain allowed.
- User prefers evidence-first trading-system evaluation and iterative improvement in this order: (1) increase sample accumulation quality, (2) strengthen predictability-score to real-PnL linkage, (3) reduce aggressiveness and focus majors first, (4) align performance/validation/accounting metrics, (5) automate symbol-specific promotion/demotion.
- Completed repo changes and commits on 2026-03-18:
  - `0437ca9` — disable auto summaries and refine warmup validation behavior.
  - `2ad4ad3` — add weekly validation sample progress tracking.
  - `927f58d` — track score-to-PnL alignment in validation reports.
- Important testing caveat: with local operator setting `TELEGRAM_REPORT_ONLY=1`, alert-sending session tests fail by design; for code regression verification, run tests with env override `TELEGRAM_REPORT_ONLY=0`.
- Latest assessment stance to preserve: the bot is not a hopeless program, but still `needs work`; engineering/operational structure is decent, yet live sample size and repeated profitability evidence are still insufficient for calling it a fully proven high-quality profitable system.
- 2026-03-18: 추가 완료 커밋 보존. `18ee031` — `Keep aggressive scaling focused on majors`; 비메이저 `aggressive_pass`/`strong_pass`에서 공격적 증액을 막고 보수 스케일만 허용. `a524b6d` — `Filter non-report telegram alerts in report-only mode`; `send_telegram_message(text: str)`에서 `REPORT_ONLY_PREFIXES` / `is_report_message(text)` 기반 allowlist로 보고서 외 운영 알림을 발송 단계에서 차단. `211e266` — `Stop early on severe live execution failures`; 초기 실거래 표본이 적더라도 손실/리젝트가 심각하면 신규 진입을 막는 self-correction/cap 경로 반영.
- 2026-03-18: observe-only/demote 연결 강화 완료. `quant_binance/observability/report.py`에서 `_runtime_decomposition_adjustments(...)`가 `runtime_evidence["observe_only_symbols"]`를 읽도록 바뀌었고, 새 `_observe_only_runtime_adjustments(runtime_evidence)`가 `observe_only_symbols`에 대해 `action="demote"`, `reason="RUNTIME_OBSERVE_ONLY_SYMBOL"`, `signal_source="runtime_observe_only"`, `score_delta=-0.18`(alt) / `-0.08`(major) 정책 조정을 생성. 비메이저 alt가 `observe_only` 상태이면 `symbol_summary` 기반 `promote`를 막도록 연결됨. 관련 테스트: `test_auto_tune_policy_demotes_observe_only_alt_and_blocks_promote`, `test_runtime_summary_lists_observe_only_symbols`. 회귀: `TELEGRAM_REPORT_ONLY=0 python3 -m unittest tests.test_quant_binance_core tests.test_quant_binance_session tests.test_quant_binance_validation_report tests.test_quant_binance_performance_report tests.test_quant_binance_telegram_reports tests.test_quant_binance_telegram_notify tests.test_telegram_notify` → `Ran 190 tests in 33.329s`, `OK`. 커밋: `89188a3` — `Demote observe-only alts in auto tune policy`.
- 2026-03-18: 다음 진행축은 4순위 `performance_report / validation_report / runtime summary / closed_trades` 사이의 집계 기준 통일. 현재 판단: 같은 `closed trade / realized pnl / symbol performance`를 서로 다른 경로(`logs/closed_trades.jsonl`, 메모리상의 `closed_trades`, `summary.json` 혼합)로 집계하고 있어 숫자 어긋남 가능성이 큼. 아직 구현/커밋 전이며, 우선 어긋남 포인트를 좁히는 분석 단계까지 진행됨.
