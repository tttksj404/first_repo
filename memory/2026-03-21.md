## 2026-03-21
- Quant runtime: changed `quant_binance/session.py` so futures live sizing uses `futures_total_reusable_balance_usd` / `futures_recognized_balance_usd` as fallback beyond `futures_execution_balance_usd`; added test `test_futures_entry_uses_total_reusable_balance_when_execution_balance_is_low`; committed as `6a1235c` (`Use reusable futures balance for live sizing`).
- Quant runtime verification after restart: supervisor restarted with `QUANT_TELEGRAM_NOTIFICATIONS=0` and `QUANT_BYPASS_POLICY_GUARDRAILS=1`; `summary.state.json` showed `decision_count: 945`, `live_order_count: 1`, `tested_order_count: 1`; `live_supervisor_health.json` showed `status: healthy`; repeated `ETHUSDT existing=yes` observed; websocket reconnect errors (`no close frame received or sent`) were non-fatal and runtime recovered.
- User asked about TOEIC 900+; preference/context: wants a concrete plan for scoring well above 900, likely practical study plan rather than theory-heavy explanation.
