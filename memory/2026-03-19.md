- User confirmed operational preference: do not send stop-loss/daily/intraday style operational alerts to Telegram; internal autonomous judgment is preferred, while report-only messaging remains acceptable.
- Runtime was explicitly restarted on 2026-03-19 after confirmation, using `sh scripts/quant_run_live_orders.sh quant_runtime`; prior daemon state was stale (last meaningful state from 2026-03-18), then new run came up healthy with fresh counters and zero live/tested orders initially.
- Stopping the old runtime sent one allowlisted report message (`[오픈클로 실거래 요약 리포트] 중지`) to Telegram; user preference still remains report-only, not operational chatter.
- Latest confirmed repo state for quant work included commit `345f789` (`Prioritize top-ranked symbol promotions`) after focused tests passed: `TELEGRAM_REPORT_ONLY=0 python3 -m unittest tests.test_quant_binance_core tests.test_quant_binance_session -q` -> `Ran 178 tests ... OK`.
- Current evidence-based evaluation of the trading system should remain conservative: stronger structure and automation than many simple bots, but still `needs work`, not objectively proven as a profit-maximizing or fully production-ready system due to limited live sample / closed-trade evidence.
- User is interested in next-stage automation: automate sample-quality monitoring and use that monitoring to automatically improve decision accuracy via policy adjustments, rather than merely waiting for more live data.
- Recommended next implementation direction: `sample-quality watchdog` + connect watchdog outputs to policy auto-adjust (observe-only/demote/intensity/top-k gating) + checkpoint-triggered revalidation, preferably delegated to Codex as main executor.
- 2026-03-19 18:40 KST: Continued quant_binance automation stream with direct verification/commit closure after each Codex run. New committed milestones today after prior baseline/watchdog/checkpoint work: `8f39c61` (`Automate symbol lifecycle guardrails`), `993ef22` (`Add regime-aware auto mode guidance`), `a9ba7f5` (`Separate policy-lineage evidence attribution`). Current next active Codex session: `tide-meadow`, tasked with adding a top-level executive verdict layer that consolidates watchdog/execution-quality/checkpoint/baseline/lifecycle/lineage/auto-mode into conservative machine-readable operating verdicts (`expand/hold/tighten/rollback/rebuild_evidence`). User preference remains evidence-first, conservative, majors-first, Codex-first for large implementation, and only report completion after real tests + commit. Never modify `.env.local`; commit only related files; exclude `_meta/career_competency_log.md` from these focused commits unless explicitly intended.
