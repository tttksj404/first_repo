- Quant live trading work on 2026-03-10: diagnosed no-trade issue as overly strict futures gating, then revised strategy toward more frequent entries with post-entry risk management. Added stronger-setup leverage path, thinner-edge futures filtering (`EDGE_TOO_THIN`), balance-aware live order capping, quoteOrderQty handling for spot market buys, bootstrap account sync before first live trade, and skipping sub-minimum balance live/test orders.
- Example scenario checks after revisions: mild futures setups now route to spot when edge is too thin; strong futures setups remain allowed and are sized larger; bad macro/heat setups are prevented from futures.
- Live execution observations: at least one spot BTC buy appears to have executed during debugging (spot balances moved from ~33.83 USDT to ~4.19 USDT and BTC balance became ~0.00042957). Remaining spot balance then fell below Binance minimum notional, motivating the sub-minimum skip guard.
- Relevant commits made in this session: `9250a33` (rebalance live futures entry and sizing), `a2b0af8` (support spot market buy quote sizing), `48d51b3` (cap live orders to available balances), `76cfa82` (sync account before bootstrap trading), `f852753` (skip sub-minimum balance live and test orders), plus earlier note/memo commit `346c5a6`.
- User preference captured: prefers strategies that enter more readily, then manage profit/risk during operation; okay with bolder leverage only when objectively justified by prior crawled strategy materials and strong setup evidence; wants this thinking preserved for later review.
