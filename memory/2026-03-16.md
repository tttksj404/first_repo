- Quant runtime ops: diagnosed missing Telegram delivery loop for overnight strategy advisor reports; wired `scripts/quant_run_live_orders.sh` to start an immediate + every-4-hours advisor cycle via `scripts/quant_strategy_advisor_cycle.sh` with Telegram send enabled by default. Commit: `9a84e2f`.
- Runtime status check at ~2026-03-16 07:18 KST showed live supervisor healthy, runtime process alive, heartbeat/summary.state updating, but no active positions because strategy filters were rejecting entries rather than due to process failure or missing capital.
- Trading assessment: current futures non-entry judged reasonable given positive expected edge but tiny realized sample, high rejection reasons (`SENTIMENT_CAUTION`, `SUPPORT_NOT_CONFIRMED`, `SENTIMENT_TOO_WEAK`, `BUYING_INTO_RESISTANCE`, `EDGE_TOO_THIN`), and upcoming macro event risk; advised against lowering core cost/edge/resistance guards.
- Spot/funding architecture finding: system recognized BTC inside futures collateral/equity for USDT-margined futures, but did not previously support using held non-USDT spot assets as funding sources for other spot buys or coin-to-coin style asset utilization.
- Implemented multi-asset spot funding groundwork so spot capital recognition and execution routing can use held non-USDT assets (especially BTC) as funding sources instead of assuming only USDT-funded spot buys. Commit: `89896f8` (`Enable multi-asset spot funding routes`). Verified with `python3 -m unittest tests.test_quant_binance_capital tests.test_quant_binance_live_orders tests.test_quant_bitget_migration tests.test_quant_binance_daemon` -> 53 tests OK.
- Verified and committed macro regime risk overlay/runtime reporting across quant_binance macro layer files (`quant_binance/models.py`, `quant_binance/overlays.py`, `quant_binance/strategy/regime.py`, `quant_binance/session.py`, plus 2 tests). Commit: `1b6a442` (`Add macro regime risk overlay and runtime reporting`). Validation: `python3 -m unittest tests.test_quant_binance_overlays tests.test_quant_binance_strategy_profile` -> 23 tests OK; `python3 -m py_compile quant_binance/models.py quant_binance/overlays.py quant_binance/strategy/regime.py quant_binance/session.py` OK.
- Strategy benchmarking conclusion: users crypto system is strongest as a medium-frequency filtered risk-aware strategy; biggest next-step gains versus stronger commercial systems are portfolio-level exposure control, execution-quality feedback/adaptation, tiered symbol policies (majors vs alts), richer position management, stronger event-window macro modes, and capital allocator/orchestration improvements. Avoid copying naive macro triggers, overfit indicator soups, and uniform treatment of small alts.
