- Built a reusable local reference stack under `04. Tools/agent-stack/` for GitHub/web resources the user shared, including sync tooling (`scripts/agent_stack_sync.sh`), Codex repo query wrapper (`scripts/codex_agent_stack.sh`), a catalog, and routing docs.
- Added natural-language routing and dispatch tooling for broad Telegram/OpenClaw requests: `scripts/nl_route.py`, `scripts/nl_dispatch.py`, `docs/nl-routing-registry.md`, plus guidance updates in repo docs so broad requests default to the dispatcher heuristic before tool choice.
- Confirmed the user wants autonomous routing of requests to the best skill/reference/Codex path with minimal prompting; broad requests should usually be classified first, then handled directly or delegated.
- Investigated `dot-studio` as the Figma-like drag-and-drop orchestration candidate. Conclusion: it is a visual orchestration UI, but currently OpenCode-centered rather than native Codex/Gemini.
- Built a prototype bridge at `04. Tools/agent-stack/dot_studio_bridge/` so DOT Studio-style orchestration can call existing local wrappers for `codex` and `gemini`. Includes single-step execution (`run_provider_task.py`), serial sequence execution with template substitution (`run_sequence.py`), examples, and an architecture note for Studio-side integration.
- Verified bridge operation end-to-end: Codex single-step, Gemini single-step, and Gemini→Codex serial sequence all ran successfully via existing wrapper scripts.
- Relevant commits made during this stretch:
  - `c85155d` Add reusable agent stack tooling
  - `912c9fa` Add natural language routing registry
  - `098fde4` Add semi-automatic route dispatcher
  - `de34dcb` Use dispatcher as default routing heuristic
  - `083cdfc` Add DOT Studio Codex and Gemini bridge prototype
- Noted environment noise during provider runs: Codex/Gemini tasks succeeded, but stderr showed Notion MCP credential refresh failures and some OMX MCP startup failures; bridge logic itself was not blocked by these issues.
