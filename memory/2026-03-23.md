- Quant runtime recovery hardening on 2026-03-23: identified remaining restart instability as watchdog/supervisor python path propagation mismatch, not just duplicate processes. Added stronger self-healing around `scripts/quant_run_live_orders.sh` and `scripts/quant_live_watchdog.sh`.
- Confirmed evidence before compaction: single active process set achieved temporarily with `sh scripts/quant_run_live_orders.sh quant_runtime`, `sh scripts/quant_live_watchdog.sh quant_runtime`, and `python -m quant_binance.runtime --mode live-auto-trade-daemon --exchange bitget --output-base quant_runtime`; `quant_runtime/live_supervisor_health.json.status = healthy`; `quant_runtime/output/paper-live-shell/latest/summary.state.json.updated_at` continued advancing; `tested_order_count` reached `1` and log contained `LIVE_ORDER` / `TEST_ORDER` evidence.
- Remaining operational risk before flush: intermittent `scripts/quant_run_live_orders.sh: 134: python3: not found`, `:150: python3: not found`, `:226: python3: not found` still appeared in `quant_runtime/live_supervisor.log`, along with `[SUPERVISOR] child exited, restarting in 5s`, meaning restart-path instability was reduced but not yet proven eliminated.
- Root cause refinement captured: watchdog restart path had been launching supervisor without robustly preserving `PYTHON_BIN`; old duplicate watchdog/supervisor processes also muddied verification. Patched watchdog `START_CMD` to pass `PYTHON_BIN`, exported `PYTHON_BIN` in both watchdog and supervisor, and added startup logging of actual python path used so future failures can be tied to concrete resolver state.
- Added singleton-style guards: supervisor exits early if `quant_runtime/live_supervisor.pid` points to a live supervisor; watchdog exits early if `quant_runtime/live_supervisor_watchdog.pid` points to a live watchdog. This was meant to prevent future multi-watchdog/multi-supervisor drift.
- Important commit during this work: `559a379` — `Harden quant self-healing python path propagation`.
- Earlier same-day related commit still relevant: `9112f2a` — `Harden quant runtime recovery and relax major entry floors`.
- User intent to preserve: wants autonomous prevention of repeat runtime failures (“리스크 해결위한 자체 복구 자동화를 해놔”), specifically stronger always-on recovery for supervisor death, stale summary state, python path/env mismatch, and duplicate restart loops. Evidence-first verification remains required before claiming fully fixed.

## 2026-03-23 10:15 KST quant runtime recovery follow-up
- Verified remaining instability was centered on watchdog restart path and orphan supervisor sets, not just strategy logic.
- Added crash hardening for validation artifacts earlier in session and then tightened watchdog restart mechanics. New commits during this phase: `b019e34` (`Harden watchdog supervisor restart path`), `c2e8564` (`Remove unsafe python fallback from quant runtime scripts`), `0ca8454` (`Make watchdog recover supervisor pid drift`).
- `scripts/quant_live_watchdog.sh` now restarts supervisor via direct `env ... sh scripts/quant_run_live_orders.sh` instead of fragile `sh -c "$START_CMD"`, logs `python_bin` on restart, re-discovers live supervisor via `pgrep` when pid file drifts, and injects a strong PATH on restart.
- Removed unsafe bare `python3` fallback from `scripts/quant_run_live_orders.sh`, `scripts/quant_live_watchdog.sh`, and `scripts/quant_run_forever.sh`; resolver now fails loudly with `[BOOT] python resolver failed ...` instead of silently falling back to missing `python3`.
- Host runtime was repeatedly cleaned to a single active set; latest verified live set after cleanup/restart was supervisor `30166`, watchdog `30167`, child `30183`.
- Latest verified runtime evidence before flush: `quant_runtime/live_supervisor_health.json.updated_at = 2026-03-23T01:06:22.264722+00:00`, `heartbeat_count = 32327`, `decision_count = 4`, `status = healthy`; `quant_runtime/output/paper-live-shell/latest/summary.state.json.updated_at = 2026-03-23T01:07:06.902978+00:00`, `heartbeat_count = 6099`, `decision_count = 2`, `last_decision_emitted_at = 2026-03-23T01:06:51.586825+00:00`, `live_order_count = 0`, `tested_order_count = 0`.
- Important unresolved gap: historical log still showed repeated watchdog-triggered `[BOOT] python resolver failed in scripts/quant_run_live_orders.sh` at `2026-03-23 00:59:36 UTC`, `01:01:21 UTC`, `01:03:06 UTC`, `01:04:51 UTC`, `01:06:36 UTC`. After latest patch, newest visible line was `[SUPERVISOR] python_bin=/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 at 2026-03-23 10:06:48 KST`, but absence of further resolver failures still needed another short observation window.
- User intent remains: keep going autonomously until watchdog/self-healing is genuinely hardened; then separately analyze why new positions are not opening (strategy/execution rejection reasons such as `EXECUTION_QUALITY_RESTRAINT`).

## 2026-03-23 10:44 KST quant watchdog restart-path hardening follow-up
- User reaffirmed goal: not just running, but truly error-free runtime recovery.
- Verified during this segment that remaining instability is not the host Python binary itself:
  - `/Library/Frameworks/Python.framework/Versions/3.14/bin/python3` exists, passes `-x`, and runs.
  - `/usr/bin/python3` also exists, passes `-x`, and runs.
- Applied additional runtime-script hardening commits in `/Users/tttksj/first_repo`:
  - `632a220` — `Simplify quant python resolver on host`
  - `9bf0d0c` — `Avoid watchdog restarts while summary is fresh`
  - `d8a91eb` — `Bootstrap host python env in quant runtime scripts`
  - `b490a1d` — `Hard-pin host python for quant runtime scripts`
- Latest validated live set after restart in this segment:
  - supervisor `71402`
  - watchdog `71403`
  - child `71417`
- Fresh runtime evidence observed after latest restart:
  - `quant_runtime/live_supervisor_health.json.updated_at = 2026-03-23T01:33:08.197824+00:00`
  - `heartbeat_count = 7962`
  - `decision_count = 2`
  - `last_decision_emitted_at = 2026-03-23T01:31:51.324396+00:00`
  - `status = healthy`
  - `quant_runtime/output/paper-live-shell/latest/summary.state.json.updated_at = 2026-03-23T01:33:23.203846+00:00`
  - `heartbeat_count = 8390`
  - `decision_count = 2`
  - `last_decision_emitted_at = 2026-03-23T01:31:51.324396+00:00`
  - `live_order_count = 0`
  - `tested_order_count = 0`
- However, error-free status is still NOT proven. New evidence from latest log tail shows watchdog-triggered restart path still broken:
  - `1746400:[WATCHDOG] supervisor missing at 2026-03-23 01:32:51 UTC`
  - `1746402:[BOOT] fixed python bootstrap failed in scripts/quant_run_live_orders.sh at 2026-03-23 01:32:51 UTC PATH=/Library/Frameworks/Python.framework/Versions/3.14/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:...`
- This narrowed the root cause further:
  - no longer primarily a Python binary lookup issue,
  - now primarily a watchdog restart-chain / supervisor-missing false-positive design issue.
- Most recent working conclusion before compaction:
  - next fix should simplify watchdog recovery path itself,
  - prefer absolute `/bin/sh` + absolute repo script path,
  - and potentially bypass supervisor wrapper entirely for recovery if needed.
## 2026-03-23 11:39 KST - quant watchdog long-run verification failed, root cause narrowed further
- User explicitly asked: "최종 무에러까지 만들어놔" after earlier short-window verification looked clean.
- I continued evidence-first verification and found short-window success was misleading: over longer observation, watchdog still emitted repeated false restart attempts while runtime stayed alive.
- Verified active runtime at observation end:
  - supervisor pid `22400`
  - child pid `22408`
  - watchdog pid `49045`
- Verified fresh state even while restart-path errors continued:
  - `quant_runtime/live_supervisor_health.json` => `updated_at=2026-03-23T02:27:46.364200+00:00`, `heartbeat_count=90053`, `decision_count=50`, `last_decision_emitted_at=2026-03-23T02:25:35.951622+00:00`, `status=healthy`
  - `quant_runtime/output/paper-live-shell/latest/summary.state.json` => `updated_at=2026-03-23T02:28:01.330272+00:00`, `heartbeat_count=91427`, `decision_count=54`, `last_decision_emitted_at=2026-03-23T02:27:57.400276+00:00`
- Long-run log evidence proved restart-path instability is still real:
  - `1756061:[WATCHDOG] supervisor missing at 2026-03-23 02:27:07 UTC`
  - `1756062:[WATCHDOG] restarting supervisor at 2026-03-23 02:27:07 UTC`
  - `1756063:[BOOT] fixed python bootstrap failed in scripts/quant_run_live_orders.sh at 2026-03-23 02:27:07 UTC ...`
- Same failure pattern repeated across many cycles between roughly `02:13:07 UTC` and `02:27:07 UTC`, so final "error-free" claim is not supportable.
- Key diagnosis update:
  - Runtime itself can stay healthy/fresh while watchdog still false-detects `supervisor missing`.
  - Therefore remaining root cause is not host Python availability but watchdog design dependence on supervisor PID / wrapper presence.
  - Next fix direction should be stronger than incremental `supervisor_alive()` tweaks: redesign watchdog to treat `child_alive && summary_fresh` as healthy, and only recover on `child dead` or stale/missing summary.
- Code changes made during this segment:
  - Added then reverted a fragile watchdog singleton process-scan startup guard after it self-misidentified and killed valid startup attempts.
  - Commits created:
    - `dd9564f` — `Prevent duplicate quant watchdog startup`
    - `71cf41b` — `Remove fragile watchdog singleton process scan`
- Final truth at flush time:
  - single active set achieved transiently and runtime was running,
  - but watchdog false-positive restart chain still exists,
  - so user requirement "최종 무에러" remains unmet.
- Quant runtime follow-up on 2026-03-23 14:20 KST: watchdog was rewritten to gate on `child_alive && summary_fresh` rather than supervisor wrapper PID. Commit applied: `3a85701` (`Make quant watchdog gate on child and summary`). Observed active runtime set after relaunch: supervisor `22400`, child `22408`, watchdog `3497`. Short-window evidence after new watchdog start showed no immediate recurrence of `supervisor missing` / `restarting supervisor` / `fixed python bootstrap failed`, but long-run error-free status was not yet claimed.
- Verified from runtime state/logs that the engine was alive while new entries were still blocked. Latest sampled state showed `quant_runtime/live_supervisor_health.json` updated at `2026-03-23T03:24:02.146840+00:00`, `heartbeat_count=298263`, `decision_count=840`, status `healthy`; `quant_runtime/output/paper-live-shell/latest/summary.state.json` updated at `2026-03-23T03:24:17.211001+00:00`, `heartbeat_count=299479`, `decision_count=855`, `live_order_count=0`, `tested_order_count=0`.
- Durable trading diagnosis split was confirmed: ETH and SOL were blocked for different reasons. ETH repeatedly logged `[SUBMISSION_GATE] ETHUSDT allow=False ... existing=yes ...` with `[EXEC_PREFLIGHT] ... reasons=none`, meaning runtime believed an ETH futures position already existed and therefore blocked fresh entry while allowing management/exit behavior. SOL repeatedly logged `existing=no` but `[EXEC_PREFLIGHT] ... reasons=EXECUTION_QUALITY_RESTRAINT`, so no active position yet entry was denied by execution-quality gating.
- User-facing explanation preserved: “ETH 신규 진입이 안 되는 이유 = existing=yes; SOL 신규 진입이 안 되는 이유 = EXECUTION_QUALITY_RESTRAINT.” This distinction should be kept in future follow-ups instead of treating all non-entry as one issue.
- Current major profit-protection / take-profit override confirmed from artifacts: `major_partial_exit_fraction = 0.6`, `major_profit_protection_arm_roe_percent = 5.0`, `major_profit_protection_retrace_roe_percent = 1.5` in both `quant_runtime/artifacts/strategy_override.approved.json` and `quant_runtime/artifacts/strategy_advisor.context.json`. Operational meaning preserved: for major coins, arm profit protection at +5.0% ROE, then on 1.5% ROE retrace take 60% partial profit and keep 40% runner.

## 2026-03-23 14:37 KST
- User asked \"그럼어떻게하라고\" after coin-by-coin gate analysis. Durable recommended triage order established from log evidence: (1) ETHUSDT `existing=yes` state/mismatch investigation first, because `reasons=none` but 신규 진입 blocked by existing-position recognition; (2) BTCUSDT `manual_cooldown=True` trigger trace next, distinguishing manual cooldown from order cooldown and only relaxing if over-protective; (3) SOLUSDT `EXECUTION_QUALITY_RESTRAINT` condition breakdown and possible gate relaxation; (4) DOGEUSDT/TRXUSDT later because recent logs show no `SUBMISSION_GATE`/`EXEC_PREFLIGHT`, i.e. weak recent candidacy rather than active blocking.
- Durable current per-symbol summary from `quant_runtime/live_supervisor.log`: ETHUSDT recently candidate but blocked by `existing=yes` with `reasons=none`; SOLUSDT candidate but blocked by `EXECUTION_QUALITY_RESTRAINT`; BTCUSDT historically ordered but currently blocked by `manual_cooldown=True` plus `MACRO_RISK_HIGH` / sometimes `DIRECTION_CONFLICT` and `SENTIMENT_CAUTION`; DOGEUSDT and TRXUSDT have historical `LIVE_ORDER accepted=True` evidence but no recent gate/preflight lines.

## 2026-03-23 14:52 KST
- BTCUSDT 비진입 원인 분석을 코드/로그 기준으로 추가 압축.
- 확정한 사실:
  - BTCUSDT는 `order_cooldown=False`, `manual_cooldown=True` 상태로 막힘.
  - 첫 전환 지점은 `quant_runtime/live_supervisor.log` 기준 `1577618:[SUBMISSION_GATE] BTCUSDT allow=False can_open=False existing=no pending_external=True order_cooldown=False manual_cooldown=True live_allowed=True`.
  - 직전 구간(`1577612`, `1577615`)은 `existing=yes`, `manual_cooldown=False`였으므로, 기존 포지션 상태에서 포지션 정리/전환 직후 재진입 제한으로 넘어간 것으로 봄.
  - 따라서 BTC의 현재 쿨다운은 order error cooldown(`BITGET_MAX_OPEN_ZERO`, `BITGET_MAX_OPEN_BELOW_MIN_QTY`)이 아니라 close/reentry-protection 계열 cooldown임.
- 코드 근거 재확인:
  - `quant_binance/session.py` 의 `manual_symbol_cooldowns` 세팅 경로는 `_cleanup_missing_on_exchange_position(...)`, `_close_position(...)` 내 `CAPITAL_REALLOCATION`, major futures symbol의 `SIGNAL_REVERSAL`, `SCORE_DROP_EXIT`, `ENTRY_CONFIRMATION_FAILED`, `LIQUIDITY_DROP_EXIT`, 그리고 major loss reentry cooldown.
- 아직 미확정:
  - BTCUSDT의 정확한 trigger reason 문자열(`CAPITAL_REALLOCATION` vs `LIQUIDITY_DROP_EXIT` vs `SIGNAL_REVERSAL` 등)은 해당 시점 전후 로그/아티팩트에서 아직 직접 못 박지 못함.
- 사용자-facing durable summary:
  - “BTC는 주문 에러 쿨다운이 아니라, 포지션 정리 직후 걸린 manual reentry cooldown 때문에 신규 진입이 막혀 있다.”
## 2026-03-23 15:48 KST - execution quality sparse-sample halt relaxation
- `quant_binance/execution_quality.py` halt 조건을 sparse sample 완화 방향으로 확정:
  - `reject_timeout_rate >= 0.6` -> `rounded_sample_size >= 5 and reject_timeout_rate >= 0.6`
  - `avg_fill_ratio < 0.35` -> `rounded_sample_size >= 5 and effective.avg_fill_ratio < 0.35`
  - `avg_realized_edge_bps < -2.0`, `protection_degraded_rate >= 0.75` 도 `rounded_sample_size >= 5` gate로 통일.
  - `effective.avg_slippage_bps > 20.0` 는 즉시 halt 유지.
- 의도 확정:
  - 표본 3개 수준에서는 `execution_quality_halt`로 바로 0배수 차단하지 않음.
  - 초기 구간은 size reduction / leverage reduction / threshold tightening만 적용.
  - bad execution이 더 오래 누적될 때만 halt.
- 테스트 갱신 및 통과:
  - `tests/test_quant_binance_execution_quality_overlay.py`
  - `python3 -m unittest tests.test_quant_binance_execution_quality_overlay` => `Ran 14 tests ... OK`
- 주의점:
  - 일부 테스트 실패 원인은 코드가 아니라 `state.apply_overlay(...)` 호출에서 `now=`를 안 넘겨 현재시각 decay가 섞인 것이었음.
  - policy bucket snapshot에서는 sparse-sample degrade-only overlay가 `degraded_overlays` / `active_overlays`에 따로 안 잡히고 `contexts[*]["overlay"]`에만 남는 serialize 형태를 관측함.
- commit:
  - `44adbf0` — `Relax early execution quality halts for sparse samples`
- 런타임 반영 메모:
  - stale watchdog `84432`가 남아 restart path를 오염시켜 첫 반영 확인이 실패했음.
  - clean restart 후 active set:
    - supervisor `27855`
    - child `27863`
    - watchdog `28487`
- clean restart 후 핵심 증거:
  - `quant_runtime/live_supervisor_health.json`
    - `updated_at=2026-03-23T06:40:01.269441+00:00`
    - `heartbeat_count=5932`
    - `decision_count=2`
    - `status=healthy`
  - `quant_runtime/output/paper-live-shell/latest/summary.state.json`
    - `updated_at=2026-03-23T06:40:01.269441+00:00`
    - `heartbeat_count=5932`
    - `decision_count=2`
    - `live_order_count=1`
  - `quant_runtime/execution_quality_state.json`
    - SOL before: `trade_restraint=execution_quality_halt`, `size_multiplier=0.0`, `sample_size=3`, `effective_sample_size=2.965816`
    - SOL after clean restart: `trade_restraint=none`, `size_multiplier=0.452752`, `sample_size=4`, `effective_sample_size=3.962756`
- 결론:
  - SOL은 halt에서 degraded-only 상태로 내려왔음.
  - 즉 `0배수 차단` 병목은 제거됐고, 현재는 약 45% size multiplier 상태.
- 후속 우선순위 메모:
  - BTCUSDT: `manual_cooldown + MACRO_RISK_HIGH` 쪽 계속 추적.
  - DOGEUSDT / TRXUSDT: universe/decision/preflight 중 어디서 탈락하는지 최신 로그 기준으로 분해 필요.
  - SOLUSDT: halt 해제 후 실제 `LIVE_ORDER` / `TEST_ORDER`로 이어지는지 검증 필요.
- Quant runtime follow-up on 2026-03-23 17:16 KST: confirmed BTC blockage is not execution-quality-driven; `quant_runtime/execution_quality_state.json` still showed `BTCUSDT trade_restraint=none`, `size_multiplier=1.0`, `sample_size=8`.
- Parsed `quant_runtime/live_supervisor.log` and found BTC `manual_cooldown=True` persisted across a very long span: `TRUE_COUNT 30083`, `FIRST_TRUE_TS 2026-03-17T01:50:00+00:00`, `LAST_TRUE_TS 2026-03-19T21:10:00+00:00`, `FALSE_AFTER_LAST_TRUE 0`; recent preflight reasons were dominated by `MACRO_RISK_HIGH` and `DIRECTION_CONFLICT,MACRO_RISK_HIGH`.
- Reconfirmed code path facts in `quant_binance/session.py`: `_manual_reentry_cooldown_until(...)` is only one decision interval long, while `_cleanup_missing_on_exchange_position(...)`, `CAPITAL_REALLOCATION`, `SIGNAL_REVERSAL`, `SCORE_DROP_EXIT`, `ENTRY_CONFIRMATION_FAILED`, and `LIQUIDITY_DROP_EXIT` can all set manual symbol cooldowns; preflight and missing-on-exchange sliding prevention already existed from earlier patch.
- Added observability patch so future cooldown root cause is explicit in logs: `_set_manual_symbol_cooldown(...)` now records `source`, `action` (`set` / `extend` / `skip_active`), `current_until`, `new_until`, and `effective_until` to log store event `manual_symbol_cooldown`; callers now pass source values like `PREFLIGHT_SYMBOL_COOLDOWN`, `MANUAL_CLOSE_SYNCED`, `CAPITAL_REALLOCATION`, or exit reason.
- Commit created: `d0e0901` — `Log manual cooldown source transitions for BTC debugging`.
- Verification completed for observability patch: `python3 -m py_compile quant_binance/session.py tests/test_quant_binance_session.py` and targeted unittest run covering cooldown anti-sliding and major reentry cooldown tests returned `Ran 4 tests ... OK`.
- Runtime was relaunched afterward with `QUANT_TELEGRAM_NOTIFICATIONS=0` and `QUANT_BYPASS_POLICY_GUARDRAILS=1`; health/state stayed fresh (`live_supervisor_health.json updated_at=2026-03-23T08:08:58.513005+00:00 heartbeat_count=304266 decision_count=738 status=healthy`, `summary.state.json updated_at=2026-03-23T08:09:45.801975+00:00 heartbeat_count=9742 decision_count=3 live_order_count=0`), but stale/duplicate watchdog PID `56253` still appeared alongside new chain PIDs, so fully clean single-chain restart is still not proven.
- Next durable target remains: wait for or provoke the next BTC cooldown event and use the new `manual_symbol_cooldown` event stream to identify the real repeated source before changing cooldown or macro-risk policy itself.

## 2026-03-23 20:15 KST - quant watchdog false-positive restart follow-up
- User reaffirmed to proceed with removing the remaining watchdog error path (`했냐?` / continue until fixed).
- Verified the exact remaining failure still reproduces after prior shell watchdog hardening and clean restarts:
  - `[WATCHDOG] supervisor missing`
  - `[WATCHDOG] restarting supervisor`
  - `[BOOT] fixed python bootstrap failed in scripts/quant_run_live_orders.sh`
- Confirmed this is not just stale historical log noise: after fresh restarts, the latest active watchdog chain still emitted the same sequence.
- Read and followed the `coding-agent` skill via host path because the task had become a larger multi-file iterative coding/debugging effort.
- Spawned Codex CLI (`codex exec --full-auto ...`) in `/Users/tttksj/first_repo` to patch the watchdog path.
- Codex changed `scripts/quant_live_watchdog.sh` and `scripts/quant_run_live_orders.sh`; I then inspected, validated, committed, restarted, and re-verified.
- New patch committed:
  - `1676c6b` — `Version quant watchdog slots and bootstrap fallback`
- Contents of `1676c6b` at a high level:
  - both scripts now allow a `command -v python3` fallback before failing bootstrap;
  - watchdog pid file now stores `pid + slot_version` and can replace legacy watchdog ownership more safely;
  - supervisor pid file writing/reading uses slot-style parsing rather than raw-cat assumptions.
- Despite `1676c6b`, the remaining error still reproduced on the fresh chain:
  - watchdog `97624`
  - supervisor `97487`
  - child `97498`
  - log evidence after fresh restart:
    - `[WATCHDOG] watchdog started pid=97624 interval=60s stale=240s python_bin=/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 slot_version=v2:1451656557`
    - `[WATCHDOG] supervisor missing at 2026-03-23 10:44:12 UTC`
    - `[WATCHDOG] restarting supervisor at 2026-03-23 10:44:12 UTC`
    - `[BOOT] fixed python bootstrap failed in scripts/quant_run_live_orders.sh at 2026-03-23 10:44:12 UTC ...`
- Verified runtime remained alive/fresh even while the false-positive restart path fired:
  - `quant_runtime/live_supervisor_health.json`
    - `updated_at=2026-03-23T10:45:23.851506+00:00`
    - `heartbeat_count=15040`
    - `decision_count=4`
    - `status=healthy`
  - `quant_runtime/output/paper-live-shell/latest/summary.state.json`
    - `updated_at=2026-03-23T10:45:39.024878+00:00`
    - `heartbeat_count=16099`
    - `decision_count=4`
    - `live_order_count=0`
- Durable conclusion preserved for next turn:
  - `alive` is still evidenced, but `error-free` is still **not** achieved.
  - Remaining root issue is now narrowed to the external shell watchdog architecture itself, not just pid-file ownership.
  - Best next step is to replace the external shell watchdog with a Python watchdog (or otherwise remove shell-side false-positive restart logic entirely) rather than continue incremental shell patches.
