## 2026-03-17 00:20 KST
- Quant trading repo durable state: BTC/ETH-only entry relaxation and capped strong-setup size boost were completed and committed as `1c3536a` (`Relax major entries and boost strong setups`). Scope: soften only selected soft gates (`SENTIMENT_CAUTION`, `SENTIMENT_TOO_WEAK`, 일부 `SUPPORT_NOT_CONFIRMED`) for BTC/ETH when trend/volume/liquidity/edge/cost conditions are strong; keep hard guards unchanged (`EDGE_TOO_THIN`, `BUYING_INTO_RESISTANCE`, execution-quality halt, macro high-impact restraint, manual conversion safety).
- Current operational interpretation: position density feels too low not because of capital shortage but because soft filters dominate rejections. Evidence from `quant_runtime/output/paper-live-shell/latest/summary.json` top rejection counts: `SENTIMENT_CAUTION 2278`, `SUPPORT_NOT_CONFIRMED 2266`, `SENTIMENT_TOO_WEAK 1375`, versus much smaller hard-guard counts (`BUYING_INTO_RESISTANCE 11`, `EDGE_TOO_THIN 9`).
- User preference reinforced again: for feature requests, default workflow is implement/test/commit/report directly rather than discussing only. When reporting strategy behavior, use code/test/runtime evidence, not speculation.
- Likely next useful change if requested: BTC/ETH-limited second relaxation should focus on `SUPPORT_NOT_CONFIRMED` plus small additional `SENTIMENT_*` easing, while preserving hard guards and fee/execution/macro discipline.
- 2026-03-17 01:35 KST: BTC/ETH-specific futures confirmation gate was relaxed further in `quant_binance/strategy/regime.py` so reduced-size (`strategy_size_multiplier=0.75`) major futures candidates no longer auto-trigger `ENTRY_CONFIRMATION_REQUIRED`; regression test file `tests/test_quant_binance_strategy_profile.py` updated and `python3 -m unittest tests.test_quant_binance_strategy_profile` passed (29 tests, OK).
- Runtime evidence after restart (`quant_runtime/output/paper-live-shell/20260316-162444/logs/decisions.jsonl`): `BTCUSDT` and `ETHUSDT` now show `final_mode="futures"` with `divergence_code=""` while `SOLUSDT` still shows `ENTRY_CONFIRMATION_REQUIRED`, confirming majors-only relaxation works and alt conservatism remains.
- Despite cleared BTC/ETH confirmation, `live_order_count` remains 0 and there are still no `live_orders.jsonl`, `order_errors.jsonl`, or `execution_preflight.jsonl` entries, so the remaining bottleneck is now believed to be in session/live submission gating or live execution invocation after strategy approval, not in the strategy confirmation layer.

- 2026-03-17 10:15 KST: Live trading runtime cleaned up to a single active supervisor/runtime pair (`scripts/quant_run_live_orders.sh` -> supervisor pid 97212, runtime pid 97219). Current live/paper state is aligned with two existing futures shorts on exchange and paper: BTCUSDT short (exchange leverage 8) and ETHUSDT short (exchange leverage 9). Latest decisions still emit BTCUSDT long, ETHUSDT short, SOLUSDT short with empty divergence, but no new `live_orders.jsonl` or `order_errors.jsonl` entries are produced. Durable conclusion: current bottleneck is not confirmation/reject mismatch but existing paper/exchange-synced position management gating new submissions; next useful follow-up is to log exact per-symbol reasons for hold/add/reverse suppression when `existing_paper_position is not None`.
- 2026-03-17: User wants algorithm study in Obsidian note vault (not brain of podo) with an autonomous daily loop aimed at Samsung A-type + general company coding tests, mixing direct A-type prep with implementation-drill/accuracy problems because their main weakness is repeated implementation mistakes despite being able to write hard logic.
- 2026-03-17: Created note vault study system under note/Algorithms with master index, 90-day plan/execution map, daily notes, trackers, templates, pattern notes, implementation mistake log, and Study Strategy docs.
- 2026-03-17: Added automation script scripts/obsidian_algo_today.py and installed LaunchAgent com.tttksj.obsidian-algo-today to refresh note/Algorithms/Daily/Today.md every day at 06:00.

- 2026-03-17 15:27 KST: Built a separate Obsidian LEET daily study system in the `note` vault for July exam prep targeting 140, grounded in prior local LEET assets and validation artifacts (`generated_leet`, `notion_automation`, `_ai_context/reports`). Durable structure includes static LEET planning/tracker notes plus daily notes and `Today.md`, created via `scripts/obsidian_leet_today.py`, with LaunchAgent `com.tttksj.obsidian-leet-today` for daily refresh.
- 2026-03-17 15:27 KST: When user asks market-view questions like BTC outlook, they prefer a concise Korean answer based on multiple recent public-news signals with explicit uncertainty, not a fake-confident prediction.

- Added 2-stage news layer follow-up: bullish majors boost increased to `1.15` for supportive majors (`0e2b839`), then split bearish news into `execution-risk` vs `directional-bearish` with BTC/ETH short-bias support when directional bearish is high and execution risk is low.
- Directional bearish patch details: added `macro_directional_bearish_score` / `macro_execution_risk_score` to model+overlay flow, computed `directional_bearish_score` / `execution_risk_score` in `quant_binance/news_macro_signal.py`, wired through `quant_binance/overlays.py`, and let `quant_binance/strategy/regime.py` treat BTC/ETH bearish-trend setups as supportive when directional bearish is strong but execution-risk is not. Commit `e060bdb` — `Add directional bearish news bias for majors`.
- Verification evidence for latest news-layer changes: targeted tests passed (`Ran 42 tests ... OK`), broader suite passed (`Ran 174 tests in 41.011s`, `OK`), and forced refresh succeeded with `news_macro_signal=quant_runtime/artifacts/news_macro_signal.json`, `macro_inputs=quant_runtime/artifacts/news_macro_inputs.json`, `status=refreshed`.

## 2026-03-17 18:55 KST
- User approved implementing all three profitability-focused changes together ("3개다 해"): faster reversal/cleanup, softer `pre_event_reduce`, and BTC/ETH major alignment-first behavior.
- Implemented and committed `012a4ec` — `Improve major alignment and reduce restraint drag`.
- Patch details:
  - `quant_binance/overlays.py`: eased `pre_event_reduce` from `macro_size_multiplier 0.5 -> 0.65` and `macro_leverage_cap 2 -> 3`.
  - `quant_binance/session.py`: added/tested major cross-symbol alignment behavior so opposing non-exchange-synced BTC/ETH strategy paper futures positions can be closed first before new aligned major entry; added faster major reversal alignment handling to reduce confirmation/holding drag.
  - tests updated in `tests/test_quant_binance_overlays.py` and `tests/test_quant_binance_session.py`.
- Verification completed:
  - `python3 -m unittest tests.test_quant_binance_session tests.test_quant_binance_overlays tests.test_quant_binance_strategy_profile tests.test_quant_binance_live_orders`
  - Result: `Ran 172 tests in 45.311s`, `OK`.
- Initial runtime inspection right after commit showed old live process still using stale behavior/log values:
  - BTC kept hitting `EXISTING_SIDE_CONFLICT` / `HOLD_EXISTING_POSITION`.
  - decisions still showed `macro_trade_restraint: "pre_event_reduce"`, `macro_size_multiplier: 0.5`, `macro_leverage_cap: 2`.
  - Conclusion: code changed, but live runtime had not yet been restarted onto new code.
- Live runtime was then restarted with:
  - `pkill -f 'quant_binance.runtime --mode live-auto-trade-daemon' || true; pkill -f 'sh scripts/quant_run_live_orders.sh quant_runtime' || true; sleep 3; nohup sh scripts/quant_run_live_orders.sh quant_runtime >/tmp/quant_run_live_orders.post_patch.out 2>&1 & echo $!`
- After restart, new run dir confirmed: `quant_runtime/output/paper-live-shell/20260317-093715` and health remained `status: healthy`.
- Post-restart runtime evidence confirmed new behavior was active:
  - latest decisions showed `macro_trade_restraint: "none"`, `macro_size_multiplier: 1.0`, `macro_leverage_cap: 0` instead of stale pre-restart values.
  - example recovered order strength: `ETHUSDT long` with `order_intent_notional_usd ≈ 3982.86`, `net_expected_edge_bps ≈ 27.98`, `predictability_score ≈ 73.89`; `BTCUSDT long` around `4005.53` notional.
- Latest conclusion communicated to user:
  - major requested patches are done and live-reflected;
  - main remaining operational bottleneck is BTC existing hold/reversal management, not macro over-restraint anymore.
- User asked if everything was done; answer was effectively: core work is done, remaining 5% is BTC hold/reversal tuning if they want it.
## 2026-03-17 19:45 KST
- Prediction-first refactor Phase 1 was completed and committed earlier as `60b8e05` (`Add phase one prediction artifacts`). Durable architecture state: `quant_binance/models.py` now includes `ModePrediction` and `StrategyPrediction`; `quant_binance/strategy/regime.py` builds prediction artifacts first via `_mode_prediction_from_features(...)` and `build_strategy_prediction(...)`; observability exposes prediction data through `quant_binance/observability/decision_log.py` and `quant_binance/observability/report.py`.
- User explicitly wants the full staged migration to continue beyond Phase 1: next durable priority is Phase 2 `portfolio policy` separation (`prediction -> PortfolioIntent -> final decision`), followed later by execution policy split and prediction outcome audit.
- Phase 2 was started but is NOT verified complete. Durable partial-work state:
  - `quant_binance/models.py` has `PortfolioIntent` added.
  - `quant_binance/policy/portfolio.py` was created with `build_portfolio_intent(...)` and `decision_from_portfolio_intent(...)`.
  - `quant_binance/strategy/regime.py` was partially rewired so several direct `DecisionIntent` construction paths now call portfolio-policy helpers instead.
  - `tests/test_quant_binance_core.py` was partially updated to add a portfolio-intent compilation test.
- Durable blocker at end of session: execution environment degraded during verification. In exec shell, baseline tools were unavailable: `python3: not found`, `python: not found`, and `git: not found`; earlier large heredoc edit was also blocked by approval gate as `obfuscation-detected`. Because of this, Phase 2 changes could not be syntax-checked, unit-tested, diff-reviewed, or committed before compaction.
- Important truth to preserve: do NOT assume Phase 2 is complete or safe. Before any further trading/runtime use, next session should first inspect current file contents of `quant_binance/models.py`, `quant_binance/policy/portfolio.py`, `quant_binance/strategy/regime.py`, and `tests/test_quant_binance_core.py`, then re-run compile/tests in a healthy shell, fix any regression, and only then commit.

## 2026-03-17 21:28 KST
- Prediction-first refactor phases 2-5 are now materially completed and verified, not just partial. Durable commit sequence:
  - `5b3ab81` — `Add portfolio intent policy layer` (Phase 2: `prediction -> PortfolioIntent -> DecisionIntent` separation)
  - `40dfd79` — `Add execution policy seam` (Phase 3: execution boundary added via `quant_binance/policy/execution.py`; current pragmatic state keeps `ExecutionIntent = DecisionIntent` alias rather than a fully separate dataclass)
  - `ae7f1bc` — `Add execution outcome audit reporting` (Phase 4: runtime/reporting now aggregates execution outcomes such as accepted/rejected counts, fill ratio, slippage, expected vs realized edge gap, and per-symbol execution audit)
  - `e988ee3` — `Add major-specific prediction bias` (Phase 5a: BTC/ETH prediction-layer bias boost under supportive/majors-only conditions)
  - `6881c51` — `Refine major prediction and execution policy` (Phase 5b: BTC/ETH major candidate-mode selection refined toward futures when edge advantage is clear; major strong/medium futures classification helpers moved from `session.py` into `quant_binance/policy/execution.py`)
- Latest verification evidence after final fixes:
  - `/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 -m unittest tests.test_quant_binance_core tests.test_quant_binance_session tests.test_quant_binance_strategy_profile`
  - Result: `Ran 160 tests in 38.524s`, `OK`.
- Real-log profitability audit was started and produced a durable initial verdict from `quant_runtime/output/paper-live-shell/*/logs/live_orders.jsonl` and `order_errors.jsonl`:
  - observed live-order sample size was only 4 orders, all `filled`, average `fill_ratio = 1.0`, average `slippage_bps = 0.0`, average `realized_edge_bps ≈ 30.02`;
  - one durable operational risk remains: `ETHUSDT` protection-order failure in Bitget with HTTP 400 code `43059` recorded in `quant_runtime/output/paper-live-shell/20260317-073458/logs/order_errors.jsonl`;
  - expected-edge fields were missing/insufficient in older live-order rows, so the system can now report expected-vs-realized edge structurally, but real-world comparative evidence is still shallow and not enough to claim profitability validation.
- Durable user preference reinforced again: when asked to continue broad refactor/profitability work, proceed through implementation -> test -> commit with minimal chatter; final reporting should stay evidence-first in Korean with test result plus commit hash.
- Current objective truth to preserve: the strategy is now structurally prediction-first enough to evaluate and tune, but profitability is still `needs work` because long-run expected-vs-realized retention, protection-order reliability, and larger live sample validation are still pending.

## 2026-03-17 22:38 KST
- Implemented automatic operational verdict layer and then connected it to live self-correction. Durable commit sequence for this stage:
  - `4ef0b1c` — `Add runtime operational verdicts` (summary now computes `operational_verdict` from execution audit metrics)
  - `a321cb2` — `Connect operational verdicts to self-correction` (session actuation: `hold` halves notional/size multiplier, `stop` blocks new entries via `cash`/`flat`)
  - `e49cdea` — `Add adaptive strong-pass aggressiveness` (extends verdicts to `strong_pass / pass / hold / stop`; `strong_pass` increases notional and `strategy_size_multiplier` by `1.15x`)
- Durable operational design decision: verdict calculation and runtime actuation must share the same criteria, so `quant_binance/observability/report.py` now exposes public `build_operational_verdict(...)` and `quant_binance/session.py` reuses it directly instead of reading a previously written summary file.
- Durable runtime behavior after latest patch:
  - `strong_pass`: scale new-entry notional + `strategy_size_multiplier` by `1.15x`
  - `pass`: no change
  - `hold`: scale new-entry notional + `strategy_size_multiplier` by `0.5x`
  - `stop`: block new entries (`final_mode="cash"`, `side="flat"`, zero notional)
  - Important safeguard: if `live_orders` sample size is below 5, the system may still report `hold` in summary, but **no automatic actuation is applied yet**; verdict and actuation were deliberately separated to avoid breaking normal low-sample flows.
- Durable verification evidence for this stage:
  - `/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 -m unittest tests.test_quant_binance_session tests.test_quant_binance_core tests.test_quant_binance_ops tests.test_quant_binance_live_orders tests.test_quant_bitget_migration`
  - Result after final fixes: `Ran 199 tests in 44.914s`, `OK`.
- Important durable lesson from failed first attempt: applying `hold` actuation immediately on small samples caused broad regressions in session/live-order tests (futures reallocation, transfer routing, capacity release, etc.). The fix was not to weaken the verdict itself, but to gate **actuation** on sample sufficiency (`len(self.live_orders) >= 5`).
- User’s durable product direction is now explicit: a mere self-protection engine is insufficient. They want an adaptive profit-seeking system that can both protect edge when execution quality deteriorates and become more aggressive when evidence is strong. Current truth: adaptive actuation is now partially implemented; fully automatic re-learning / threshold auto-tuning is still not implemented.
